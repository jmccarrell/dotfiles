#!/usr/local/bin/bash
# search Jeffs common notes locations with the silver searcher

readonly -a source_dirs=(
    ${HOME}/jwm/notes
    ${HOME}/jwm/todo
    ${HOME}/pdata/notes
)

readonly -a other_dirs=(
    ${HOME}/pdata/employers/{sift,entelo,frontrow}/notes
)

declare -a ag_dirs=()
for d in ${source_dirs[@]}; do
    if [ -d "$d" ]; then
        ag_dirs+=("$d")
    fi
done

ag $@ ${ag_dirs[@]}
